import { LazyVerification } from "../lazy-verification.js";
export default async () => ({
    async newConnection(context, next) {
        const connection = await next(context);
        connection.verification = new LazyVerification(connection.provider, connection.networkName, context.config.chainDescriptors, context.config.verify);
        return connection;
    },
});
//# sourceMappingURL=network.js.map