import * as P from 'micro-packed';
import { type IWeb3Provider, type TArg, type TRet } from '../utils.ts';
import { type ArgsType, type ArrLike, type Component, type IsEmptyArray, type MapTuple, type NamedComponent, type Writable } from './abi-mapper.ts';
import { type ClearSigDef, type ClearSigFactoryEntry, type ClearSigOpt, type ClearSigRepositoryEntry } from './clearsig.ts';
export type FunctionType = Component<'function'> & {
    readonly inputs?: ReadonlyArray<Component<string>>;
    readonly outputs?: ReadonlyArray<Component<string>>;
};
type ContractMethodDecode<T extends FunctionType, O = ArgsType<T['outputs']>> = IsEmptyArray<T['outputs']> extends true ? {
    decodeOutput: (b: Uint8Array) => void;
} : {
    decodeOutput: (b: Uint8Array) => O;
};
type ContractMethodEncode<T extends FunctionType, I = ArgsType<T['inputs']>> = IsEmptyArray<T['inputs']> extends true ? {
    encodeInput: () => Uint8Array;
} : {
    encodeInput: (v: I) => Uint8Array;
};
type ContractMethodGas<T extends FunctionType, I = ArgsType<T['inputs']>> = IsEmptyArray<T['inputs']> extends true ? {
    estimateGas: () => Promise<bigint>;
} : {
    estimateGas: (v: I) => Promise<bigint>;
};
type ContractMethodCall<T extends FunctionType, I = ArgsType<T['inputs']>, O = ArgsType<T['outputs']>> = IsEmptyArray<T['inputs']> extends true ? IsEmptyArray<T['outputs']> extends true ? {
    call: () => Promise<void>;
} : {
    call: () => Promise<O>;
} : IsEmptyArray<T['outputs']> extends true ? {
    call: (v: I) => Promise<void>;
} : {
    call: (v: I) => Promise<O>;
};
export type ContractMethod<T extends FunctionType> = ContractMethodEncode<T> & ContractMethodDecode<T>;
export type ContractMethodNet<T extends FunctionType> = ContractMethod<T> & ContractMethodGas<T> & ContractMethodCall<T>;
export type FnArg = {
    readonly type: string;
    readonly name?: string;
    readonly components?: ArrLike<FnArg>;
    readonly inputs?: ArrLike<FnArg>;
    readonly outputs?: ArrLike<FnArg>;
    readonly anonymous?: boolean;
    readonly indexed?: boolean;
};
export type ContractTypeFilter<T> = {
    [K in keyof T]: T[K] extends FunctionType & {
        name: string;
    } ? T[K] : never;
};
export type ContractType<T extends Array<FnArg>, N, F = ContractTypeFilter<T>> = F extends ArrLike<FunctionType & {
    name: string;
}> ? {
    [K in F[number] as K['name']]: N extends IWeb3Provider ? ContractMethodNet<K> : ContractMethod<K>;
} : never;
export declare function evSigHash(o: FnArg): string;
export declare function fnSigHash(o: FnArg): string;
export declare function createContract<T extends ArrLike<FnArg>>(abi: T, net: IWeb3Provider, contract?: string): TRet<ContractType<Writable<T>, IWeb3Provider>>;
export declare function createContract<T extends ArrLike<FnArg>>(abi: T, net?: undefined, contract?: string): TRet<ContractType<Writable<T>, undefined>>;
type GetCons<T extends ArrLike<FnArg>> = Extract<T[number], {
    type: 'constructor';
}>;
type ConstructorType = Component<'constructor'> & {
    readonly inputs?: ReadonlyArray<Component<string>>;
};
type ConsArgs<T extends ConstructorType> = IsEmptyArray<T['inputs']> extends true ? undefined : ArgsType<T['inputs']>;
type DeployArgs<T extends ArrLike<FnArg>> = GetCons<T> extends never ? [args: unknown] : ConsArgs<GetCons<T>> extends undefined ? [] : [args: ConsArgs<GetCons<T>>];
export declare function deployContract<T extends ArrLike<FnArg>, A extends TArg<DeployArgs<T>> & unknown[]>(abi: T, bytecodeHex: string, ...args: A): string;
export type EventType = NamedComponent<'event'> & {
    readonly inputs: ReadonlyArray<Component<string>>;
};
export type ContractEventTypeFilter<T> = {
    [K in keyof T]: T[K] extends EventType ? T[K] : never;
};
export type TopicsValue<T> = {
    [K in keyof T]: T[K] | null;
};
export type EventArgsType<T extends ReadonlyArray<any> | undefined> = IsEmptyArray<T> extends true ? {} : T extends ReadonlyArray<any> ? MapTuple<T> : {};
export type EventMethod<T extends EventType> = {
    decode: (topics: string[], data: string) => EventArgsType<T['inputs']>;
    topics: (values: TopicsValue<EventArgsType<T['inputs']>>) => (string | null)[];
};
export type ContractEventType<T extends Array<FnArg>, F = ContractEventTypeFilter<T>> = F extends ArrLike<EventType> ? {
    [K in F[number] as K['name']]: EventMethod<K>;
} : never;
export declare function events<T extends ArrLike<FnArg>>(abi: T): TRet<ContractEventType<Writable<T>>>;
export type ContractABI = ReadonlyArray<FnArg & {
    readonly hook?: HookFn;
    readonly hint?: HintFn;
}>;
export type ContractInfo = {
    abi: 'ERC20' | 'ERC721' | 'ERC1155' | ContractABI;
    symbol?: string;
    decimals?: number;
    name?: string;
    price?: number;
};
export type HintOpt = {
    contract?: string;
    amount?: bigint;
    contractInfo?: ContractInfo;
    contracts?: Record<string, ContractInfo>;
    /** Some aggregators append referral/fingerprint bytes after ABI args. */
    allowUnreadBytes?: boolean;
};
export type HookFn = (decoder: Decoder, contract: string, info: SignatureInfo, opt: HintOpt) => SignatureInfo;
/** Compact post-transaction label for a decoded receipt log. */
export type HintFn = (value: unknown, opt: HintOpt) => string;
/** Factory-resolution options for decoder-owned ERC-7730 bindings. */
export type ClearSigResolveOpt = ClearSigOpt & {
    /** Contract instance to resolve against cached factory descriptors. */
    address: string;
    /** EIP-155 chain id; defaults to mainnet. */
    chainId?: bigint;
};
/** Decoder indexing options for ERC-7730 descriptors. */
export type ClearSigAddOpt = {
    /** Bind deployment-less descriptor entries to one concrete target for direct rendering. */
    bind?: {
        address: string;
        chainId?: bigint;
    };
};
type SignaturePacker = {
    name: string;
    signature: string;
    packer: P.CoderType<unknown>;
    hook?: HookFn;
};
type EventSignatureDecoder = {
    name: string;
    signature: string;
    decoder: (topics: string[], _data: string) => unknown;
    hint?: HintFn;
};
export type SignatureInfo = {
    name: string;
    signature: string;
    value: unknown;
    /** Optional receipt-log label; ERC-7730 signing prompts are exposed separately. */
    hint?: string;
};
/**
 * Mutable ABI decoder registry for exact contract matches and selector guesses.
 * @example
 * Decode calldata through a caller-owned registry.
 * ```ts
 * const decoder = new Decoder();
 * ```
 */
export declare class Decoder {
    contracts: Record<string, Record<string, SignaturePacker>>;
    sighashes: Record<string, SignaturePacker[]>;
    evContracts: Record<string, Record<string, EventSignatureDecoder>>;
    evSighashes: Record<string, EventSignatureDecoder[]>;
    clearSig?: {
        contracts: Record<string, Record<string, ClearSigRepositoryEntry[]>>;
        generic: ClearSigRepositoryEntry[];
    };
    clearSigFactories: ClearSigFactoryEntry[];
    clearSigResolved: Record<string, boolean>;
    private clearSigSelectors;
    private addClearSigEntry;
    /** Adds ERC-7730 descriptors to this decoder, hashing ABI selectors through the normal ABI path. */
    addClearSig(files: TArg<Record<string, ClearSigDef>>, opt?: TArg<ClearSigAddOpt>): this;
    /** Resolves factory-backed ERC-7730 descriptors into exact contract bindings. */
    resolve(opt: TArg<ClearSigResolveOpt>): Promise<boolean>;
    /** Looks up a chain-scoped ERC-7730 renderer for already-decoded calldata. */
    clearSigEntry(contract: string, data: TArg<Uint8Array>, chainId?: bigint): ClearSigRepositoryEntry | undefined;
    add(contract: string, abi: ContractABI): void;
    method(contract: string, data: Uint8Array): string | undefined;
    decode(contract: string, _data: Uint8Array, opt?: HintOpt): SignatureInfo | SignatureInfo[] | undefined;
    decodeEvent(contract: string, topics: string[], data: string, _opt: HintOpt): SignatureInfo | SignatureInfo[] | undefined;
}
export {};
//# sourceMappingURL=abi-decoder.d.ts.map